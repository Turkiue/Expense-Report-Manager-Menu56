import App.EMMenu;

// this class to start the app

public class Start {

    // Creating an object and start the app by calling service.showMenu();

    public static void main(String[] args) {
        EMMenu menu = new EMMenu();
        menu.showMenu();
    }
}